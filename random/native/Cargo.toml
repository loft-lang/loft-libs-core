[package]
name = "loft-random"
version = "0.1.0"
edition = "2024"
license = "LGPL-3.0-or-later"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
loft-ffi = { path = "../../../../loft/loft-ffi" }
rand_core = { version = "0.9", features = ["os_rng"] }
rand_pcg = "0.9"
