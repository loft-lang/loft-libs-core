[package]
name = "arguments"
version = "0.2.1"
# Requires the cursor / match-PEG engine (@PLN35); pin the exact minimum once
# that lands in a release.
loft = ">=0.8"

api_compatible_with  = "0.2.0"   # oldest own release this is a drop-in for
data_compatible_with = "0.2.0"   # oldest own release whose stored data this reads
[library]
entry = "src/arguments.loft"
